# Trivy Configuration
# Docs: https://trivy.dev/v0.33/docs/references/customization/config-file/

# Output format
format: table

# Severities that will cause the build to fail
severity:
  - MEDIUM
  - HIGH
  - CRITICAL